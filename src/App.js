import Slider from './Components/Slider.js';
import GlobalState from './GlobalState.js';

const App = () => {
    return (
        <GlobalState>
            <Slider />
        </GlobalState>
    );
};

export default App;
